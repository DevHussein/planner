apiVersion: apps/v1
kind: Deployment
metadata:
  name: planner-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: planner-lbl
  template:
    metadata:
      labels:
        app: planner-lbl
    spec:
      containers:
        - name: planner-container
          image: dockerathussein/planner-app
          ports:
            - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: planner-svc
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 32000
  selector:
    app: planner-lbl
